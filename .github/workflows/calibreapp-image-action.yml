name: Compress images
on:
  pull_request:
jobs:
  build:
    name: calibreapp/image-actions
    runs-on: ubuntu-latest
    steps:
      - name: Print Env
        env:
          PR_REPO: ${{ github.event.pull_request.head.repo.fullname }}
          THIS_REPO: ${{ github.repository.fullname }}
        run: |
           echo $PR_OWNER
           echo $THIS_REPO
           
